package com.linkedin.app;

import java.sql.Timestamp;

public class LoggerService {
  public void logMessage(String transactionId, double amount, Timestamp timestamp) {
    // Log the message with transaction details (implementation not shown)
  }
}
